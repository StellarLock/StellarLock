[package]
name = "locker-common"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Shared types, constants, and helpers for token-locker and lp-locker contracts."

[lib]
crate-type = ["rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { version = "28.0.0", features = ["testutils"] }
