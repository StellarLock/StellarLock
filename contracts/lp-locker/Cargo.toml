[package]
name = "lp-locker"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
locker-common = { path = "../locker-common" }

[dev-dependencies]
soroban-sdk = { version = "28.0.0", features = ["testutils"] }
